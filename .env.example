# AI Studio startup environment prompt disabled.
#
# KIS and Upbit credentials are registered from the app's built-in API registration UI
# and are not requested as AI Studio startup environment variables.
#
# Keep real API keys out of this repository.
