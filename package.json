{
  "name": "react-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs && node -e \"const fs = require('fs'); fs.cpSync('dist', 'build', { recursive: true });\"",
    "start": "node dist/server.cjs",
    "test": "VITE_FIREBASE_API_KEY=test-only-not-a-secret node --import tsx --test tests/*.test.ts tests/**/*.test.ts",
    "test:python": "python3 -m unittest discover -s tests/python -p 'test_*.py'",
    "clean": "rm -rf dist server.js",
    "audit:fake-data": "node scripts/check-no-fake-production-data-v4.mjs",
    "audit:boundaries": "node scripts/check-production-import-boundaries.mjs",
    "audit:buttons": "node scripts/check-production-buttons.mjs",
    "validate:v20": "node scripts/check-v20-production-truth.mjs && npm run audit:fake-data && npm run lint && npm test && npm run test:python && npm run build",
    "validate:final": "node scripts/check-final-production-truth.mjs && node scripts/check-v20-production-truth.mjs && npm run audit:fake-data && npm run audit:boundaries && npm run audit:buttons && npm run lint && npm test && npm run test:python && npm run build",
    "validate:rc5": "node scripts/check-rc5-production-truth.mjs && node scripts/check-final-production-truth.mjs && node scripts/check-v20-production-truth.mjs && npm run audit:fake-data && npm run audit:boundaries && npm run audit:buttons && npm run lint && npm test && npm run test:python && npm run build",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "@tailwindcss/vite": "^4.1.14",
    "@vitejs/plugin-react": "^5.0.4",
    "d3": "^7.9.0",
    "dotenv": "^17.2.3",
    "esbuild": "^0.25.0",
    "express": "^4.21.2",
    "firebase": "^12.16.0",
    "lightweight-charts": "^5.2.1",
    "lucide-react": "^0.546.0",
    "motion": "^12.23.24",
    "react": "^19.0.1",
    "react-dom": "^19.0.1",
    "recharts": "^3.10.1",
    "tsx": "^4.21.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.3",
    "ws": "^8.21.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/d3": "^7.4.3",
    "@types/express": "^4.17.21",
    "@types/node": "^22.14.0",
    "@types/ws": "^8.18.1",
    "autoprefixer": "^10.4.21",
    "tailwindcss": "^4.1.14"
  }
}
